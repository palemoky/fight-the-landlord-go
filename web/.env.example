# Copy this file to .env for local development.
# The browser connects to /ws on the Vite dev server; this target is used by
# Vite's dev proxy to forward /ws to the Go backend.
VITE_WS_TARGET=ws://localhost:1780
